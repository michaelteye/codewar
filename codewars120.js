function sumStr(a,b) {
    let all = Number(a) + Number(b)
    
    return all.toString()
     
}

//alternative
function sumStr(a,b) {
    let all = parseInt(a) + parseInt(b)
    
    return all.toString()
     
}